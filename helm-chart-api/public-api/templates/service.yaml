apiVersion: v1
kind: Service
metadata:
  name: {{ include "public-api.fullname" . }}
  labels:
    {{- include "public-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "public-api.selectorLabels" . | nindent 4 }}
